#!/bin/bash -e

# Prepare every production process before it starts. Hosting platforms often
# wrap or override the Docker command, so matching one exact server command can
# leave the app running against an uninitialized database.
if [ "${RAILS_ENV:-production}" == "production" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
